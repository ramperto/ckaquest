name: "Liveness Probe Failure — Wrong Path"
description: >
  A pod keeps restarting every ~30 seconds. The application itself seems
  fine, but Kubernetes keeps killing it. The team suspects the health check
  configuration is wrong.
objective: "Fix the liveness probe so the pod stays Running without unnecessary restarts."
xp: 150
difficulty: intermediate
expected_time: "15m"
concepts:
  - Liveness probes
  - Health checks
  - HTTP probe paths
  - Pod restarts
  - kubectl describe
