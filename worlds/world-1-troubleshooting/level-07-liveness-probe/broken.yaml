apiVersion: v1
kind: Pod
metadata:
  name: web
  namespace: ckaquest
  labels:
    app: web
spec:
  containers:
    - name: web
      image: nginx:1.25
      ports:
        - containerPort: 80
      livenessProbe:
        httpGet:
          path: /healthz        # nginx doesn't serve this path — returns 404
          port: 80
        initialDelaySeconds: 5
        periodSeconds: 10
        failureThreshold: 3
