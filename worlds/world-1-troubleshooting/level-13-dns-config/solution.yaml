apiVersion: v1
kind: Pod
metadata:
  name: debug-pod
  namespace: ckaquest
  labels:
    app: debug
spec:
  dnsPolicy: ClusterFirst    # Default: use CoreDNS for cluster DNS
  containers:
    - name: debug
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
