apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-v1
  namespace: ckaquest
  labels:
    app: myapp
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
      version: v1
  template:
    metadata:
      labels:
        app: myapp
        version: v1
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
            - containerPort: 80
          env:
            - name: APP_VERSION
              value: "v1"
---
# FIX: added "app: myapp" to pod template labels AND selector
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-v2
  namespace: ckaquest
  labels:
    app: myapp
    version: v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
      version: v2
  template:
    metadata:
      labels:
        app: myapp        # FIX: added this label so Service selector matches
        version: v2
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
            - containerPort: 80
          env:
            - name: APP_VERSION
              value: "v2"
---
apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
  namespace: ckaquest
spec:
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
