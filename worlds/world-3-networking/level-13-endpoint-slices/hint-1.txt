Check if the Service has any endpoints:

  kubectl get endpoints backend-svc -n ckaquest

If it shows "<none>" for addresses, no pods match the Service selector.

Compare the Service selector with the actual pod labels:

  kubectl get svc backend-svc -n ckaquest -o jsonpath='{.spec.selector}'
  kubectl get pods -n ckaquest --show-labels
