apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: ckaquest
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 80    # Fixed: matches nginx's actual listen port
